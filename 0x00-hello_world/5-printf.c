#include <stdio.h>

/**
 * main-prints the phrase with proper grammer
 * but outcome is a piece od art
 * Return: 0 if properly exited
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
