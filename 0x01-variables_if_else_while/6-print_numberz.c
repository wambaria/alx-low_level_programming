#include <stdio.h>

/**
 * main - Prints all the single digits numbers of base 10
 * Return: Always 0(succes)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putcahr('\n');
	return (0);
}
